{
  "name": "dappstore-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ markusbug/dappstore-subgraph",
    "create-local": "graph create --node http://localhost:8020/ markusbug/dappstore-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ markusbug/dappstore-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 markusbug/dappstore-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.43.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
